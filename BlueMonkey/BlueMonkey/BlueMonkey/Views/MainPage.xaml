<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="BlueMonkey.Views.MainPage"
             Title="MainPage">
  <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
    <Label Text="Logo" />
    <Button Text="Add Expense" Command="{Binding NavigationCommand}" CommandParameter="ExpensePage"/>
    <Button Text="Expense List" Command="{Binding NavigationCommand}" CommandParameter="ExpenseList"/>
    <Button Text="Report" Command="{Binding NavigationCommand}" CommandParameter="ReportPage"/>
    <Button Text="Chart" Command="{Binding NavigationCommand}" CommandParameter="ChartPage"/>
  </StackLayout>
</ContentPage>